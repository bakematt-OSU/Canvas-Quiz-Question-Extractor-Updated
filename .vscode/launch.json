{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Debug Current File (venv - WSL)",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "env": {},
            "envFile": "${workspaceFolder}/.env",
            "python": "${workspaceFolder}/.venv/bin/python"
        }
    ]
}
